version: '3.8'
services:
  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - '5173:5173'

  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - '5001:8000'
    depends_on:
      - frontend
